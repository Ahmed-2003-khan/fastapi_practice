# This __init__.py file marks the 'app' directory as a Python package
# It allows Python to treat this directory as a module that can be imported
# In larger applications, this file can be used to expose specific components
# For now, it's empty but its presence is essential for the package structure